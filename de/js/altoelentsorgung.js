document.addEventListener('DOMContentLoaded', function() {
    // AOS initialization (if animations needed)
    AOS.init({
        duration: 600,
        easing: 'ease-in-out',
        once: true,
        mirror: false
    });

    // Seite: Altöl – ggf. zukünftige Interaktionen hier ergänzen
});
